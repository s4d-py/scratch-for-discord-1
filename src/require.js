export default async function require(array,code,py) {
if(py.includes(".dingliflonshongfighner")) {
    array.push(`const smartestchatbot = require('smartestchatbot');
    const client = new smartestchatbot.Client();
    `)
}
}
